# Output of the third-party graphify tool, which this project no longer uses.
# Ignored so a stray manual run can never land in the repo; Lumos's own
# knowledge graph is derived into SQLite (see lumos/lumos/graph/).
graphify-out/

# Claude Code machine-local settings (absolute paths, personal overrides)
.claude/settings.local.json
